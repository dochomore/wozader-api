import { AccessTokenGuradGuard } from './access-token.guard';

describe('AccessTokenGuradGuard', () => {
  it('should be defined', () => {
    expect(new AccessTokenGuradGuard()).toBeDefined();
  });
});
