import { RefreshTokenGuradGuard } from './refresh-token.guard';

describe('RefreshTokenGuradGuard', () => {
  it('should be defined', () => {
    expect(new RefreshTokenGuradGuard()).toBeDefined();
  });
});
